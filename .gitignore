# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/
.vite/

# Vendored pose model and MediaPipe WASM runtime (~44 MB of binary; produced by
# `npm run vendor-assets`, shipped in the build artifact rather than in git)
web/public/models/
web/public/vendor/

# Training data and runs — personal video, never leaves the local machine
vision/finetune/data/
vision/finetune/runs/
fixtures/clips/

# Local database
*.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
.idea/
.vscode/
